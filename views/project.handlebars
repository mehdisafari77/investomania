<div class="text-center">
  <img src="{{image}}" alt="logo of project">
  <h2>{{title}}</h2>
  <p>Created by {{user.username}} on {{format_date date_created}}</p>
  <textarea>{{description}}</textarea>
</div>